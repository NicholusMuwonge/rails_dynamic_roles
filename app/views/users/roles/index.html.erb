<p style="color: green"><%= notice %></p>

<h1>Roles</h1>

<div id="roles">
  <% @roles.each do |role| %>
    <%= render role %>
    <p>
      <%= link_to "Show this role", users_role_path(role) %>
    </p>
  <% end %>
</div>

<%= link_to "New role", new_users_role_path %>
